//package com.grupp26.aquasim;
/*
import com.grupp26.aquasim.model.Aquarium;
import com.grupp26.aquasim.model.Decoration;
import com.grupp26.aquasim.model.IAquarium;
import com.grupp26.aquasim.model.Vec3;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class DecorationTest {

    private IAquarium aquarium;
    private Decoration decoration;

    @BeforeEach
    public void setup() {
        aquarium = new Aquarium();
        decoration = new Decoration(aquarium, new Vec3<>(0, 0, 0));
    }

    @Test
    public void testSetAndGetPos() {

        decoration.setPos(40,40,0);
        assertEquals(new Vec3<>(40, 40, 0), decoration.getPos());

        decoration.setPos(150,150,0);
        assertEquals(new Vec3<>(150, 150, 0), decoration.getPos());

        decoration.setPos(-50,-50,1);
        assertEquals(new Vec3<>(-50, -50, 1), decoration.getPos());

    }

}

 */
